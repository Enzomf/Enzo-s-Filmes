import styled from "styled-components"

const MediaSection = styled.section`
    width: 100%;
    margin: 2em 0 2em 0 ;
   

    svg{
        width:25px;
        margin: auto 0
    }
    `

const SectionTitle = styled.h2`
        color: #FFFF;
        margin-bottom: 0.5em;
        font-size: 2em;
        font-weight: normal ;
        display: flex;
        align-items: center;
        gap: 0.5em;
    `

export { SectionTitle, MediaSection }